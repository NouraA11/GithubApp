import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    mainContainer: {
        paddingTop: 10,
        backgroundColor: "#ffffff"
    },
    text: {
        fontSize: 18,
        textAlign: 'center',
        marginTop: 50,
    }
})